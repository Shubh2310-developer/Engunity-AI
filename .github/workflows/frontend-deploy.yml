name: Frontend Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Placeholder Deploy Step
        run: |
          echo "Implement frontend deployment here. This job is manual only (workflow_dispatch)."
